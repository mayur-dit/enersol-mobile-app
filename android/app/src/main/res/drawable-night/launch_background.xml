<?xml version="1.0" encoding="utf-8"?>
<!-- Dark-mode cold start. The mark is pre-flattened to full opacity, so it
     keeps its true colour here instead of going muddy the way the raw 70%
     artwork would. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background_night" />

    <item>
        <bitmap
            android:gravity="center"
            android:src="@drawable/splash_logo" />
    </item>
</layer-list>
